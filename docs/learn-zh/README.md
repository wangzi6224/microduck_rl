# 从零到 PPO：跟着 Microduck 学机器学习

> 一本写给"只做过 Web 前端、数学停在初中"的人的入门教材。
> 载体是本仓库：一只 25 cm 高、14 个舵机的双足机器人 Microduck，
> 用强化学习（PPO）在仿真里学会按指令走路，再部署到真机。
>
> 全书的每一个公式都从符号开始讲；每一个术语都从最根源讲；每一章末尾都把概念
> **映射到仓库里的具体文件和行号**，并配一个能直接运行的小实验来验证正文里的数字。
>
> 核实基准：分支 `develop`，commit `3412ae5`，rsl_rl_lib 5.0.1，mjlab 1.3.0，核实日期 2026-09-14。
> 行号会随代码演进漂移，遇到对不上时用 `grep -n "关键字" 文件` 重新定位。

---

## 这本书讲的是同一个故事

先把整件事用一段话说完，后面 19 章只是把这段话里的每个词讲透。

> 仿真里有 4096 只一模一样的 Microduck。每隔 0.02 秒（每秒 50 次），每只机器人
> 把自己身上能测到的 **61 个数**（陀螺仪、重力方向、14 个关节角、14 个关节速度、
> 上一次的动作、以及"你现在应该往哪走"的指令）喂给一个**神经网络**；网络吐出
> **14 个数**，作为 14 个舵机的目标角度；物理引擎往前推 0.02 秒；然后按一张
> "评分表"给这一步打一个分数（**奖励**）。每只机器人走 24 步之后，把这
> 4096 × 24 ≈ 10 万条"看到什么、做了什么、得了几分"的记录汇总起来，用一套叫
> **PPO** 的算法把网络的参数往"以后得分更高"的方向微调一点点。这个循环重复
> 几千到几万次，网络就学会了走路。最后把网络导出成一个 `.onnx` 文件，装到真机上。

```
                 ┌──────────────────────────────────────────────┐
                 │            仿真环境 (MuJoCo Warp, GPU)         │
  61 个数 ──────▶│  4096 只 Microduck 并行，每 0.02 s 一步         │──────▶ 14 个数
  (观测 obs)     │  评分表 (奖励) · 指令 (命令) · 随机扰动 (DR)      │       (动作 action)
                 └──────────────────────────────────────────────┘
                        ▲                               │
                        │ 参数微调 (PPO)                │ 24 步的记录
                        │                               ▼
                 ┌──────────────────────────────────────────────┐
                 │   神经网络 actor (61→512→256→128→14)          │
                 │   神经网络 critic (76→512→256→128→1，只训练用)  │
                 └──────────────────────────────────────────────┘
                                       │ 训练完
                                       ▼
                             policy.onnx → 真机 50 Hz
```

读完全书，上面每个加粗的词你都能：说出它的严格定义、写出它的公式、
在仓库里指出它对应的代码，以及用实验验证它。

## 目录

| 部 | 章 | 你将得到 | 实验 |
|---|---|---|---|
| **第 1 部 · 数学地基** | [01 函数与变化率](part1-math/01-函数与变化率.md) | 函数、斜率、导数、链式法则、指数函数；看懂项目的高斯奖励核 | `ch01_derivative.py` |
| | [02 向量与矩阵](part1-math/02-向量与矩阵.md) | 数组即向量、点积、长度、矩阵乘法；看懂"61 维观测"和网络第一层 | `ch02_vectors.py` |
| | [03 多变量与梯度](part1-math/03-多变量与梯度.md) | 偏导数、梯度、梯度下降、学习率、梯度裁剪 | `ch03_gradient_descent.py` |
| | [04 概率与期望](part1-math/04-概率与期望.md) | 期望、方差、大数定律、均匀/高斯分布、log-prob、熵 | `ch04_probability.py` |
| **第 2 部 · 机器学习** | [05 什么是"学习"](part2-ml/05-什么是学习.md) | 数据、模型、参数、损失、训练；线性回归闭环 | `ch05_linear_regression.py` |
| | [06 神经网络](part2-ml/06-神经网络.md) | 神经元、激活函数 ELU、MLP；数出 actor 的参数量 | `ch06_mlp.py` |
| | [07 反向传播与优化器](part2-ml/07-反向传播与优化器.md) | 计算图、autograd、SGD→Adam、mini-batch | `ch07_backprop.py` |
| | [08 输入归一化](part2-ml/08-输入归一化.md) | 为什么要 (x−μ)/σ；在线均值方差；为什么必须烘进 ONNX | `ch08_normalization.py` |
| **第 3 部 · 强化学习** | [09 强化学习的世界观](part3-rl/09-强化学习的世界观.md) | 智能体/环境/奖励/回合、MDP、策略、回报、折扣 | `ch09_gridworld.py` |
| | [10 价值函数与贝尔曼方程](part3-rl/10-价值函数与贝尔曼方程.md) | V、Q、优势、贝尔曼方程、TD 误差；critic 是什么 | `ch10_td_learning.py` |
| | [11 策略梯度](part3-rl/11-策略梯度.md) | log-derivative trick、REINFORCE、基线、高斯策略、熵 | `ch11_reinforce.py` |
| | [12 优势估计 GAE](part3-rl/12-优势估计GAE.md) | n 步回报、偏差-方差、GAE 公式；对读 rsl_rl | `ch12_gae.py` |
| | [13 PPO](part3-rl/13-PPO.md) | 重要性采样比、裁剪目标、价值损失、自适应 KL；逐行对读 ppo.py | `ch13_ppo_clip.py` |
| | [14 训练回路全貌](part3-rl/14-训练回路全貌.md) | rollout→GAE→更新；wandb 每条曲线对应的公式量 | `ch14_smoke_train.py` 🖥️ |
| **第 4 部 · 映射到 Microduck** | [15 61 维观测逐位拆解](part4-microduck/15-61维观测逐位拆解.md) | 每一位是什么物理量、噪声、延迟；actor 61 vs critic 76 | `ch15_print_obs.py` 🖥️ |
| | [16 奖励逐项拆解](part4-microduck/16-奖励逐项拆解.md) | 16 个奖励项的公式、权重与数学理由；血泪教训的数学翻译 | `ch16_reward_curves.py`（`--env` 🖥️） |
| | [17 动作、执行器与域随机化](part4-microduck/17-动作执行器与域随机化.md) | 动作→目标角、BAM 电机方程、DR 的期望含义 | `ch17_bam_motor.py` |
| | [18 课程学习与读懂 wandb](part4-microduck/18-课程学习与读懂wandb.md) | 阶梯式课程、6 个课程项时间线 | `ch18_curriculum_timeline.py` |
| | [19 导出与部署](part4-microduck/19-导出与部署.md) | ONNX、烘焙归一化、推理用均值、50 Hz 回放 | `ch19_onnx_vs_torch.py` |
| **附录** | [A 符号总表](appendix/A-符号总表.md) · [B 术语中英对照](appendix/B-术语中英对照.md) · [C 延伸阅读](appendix/C-延伸阅读.md) | | |

🖥️ = 需要 GPU（本机 RTX 5070 可跑）。其余全部 CPU，几秒钟跑完。

## 怎么读

1. **按顺序读。** 每一章开头都会说"上一章我们有了什么、这一章要补什么"，
   每个概念只在第一次出现时完整定义，之后用"→ 见 §x.y"回指。跳读会丢链。
2. **先跑实验再读正文，或者读一半就跑。** 正文里贴的每一张表都是实验脚本
   真实打印出来的；你跑出来应该一模一样（随机种子固定）。
3. **每个公式的固定讲法**：① 它在回答什么问题 → ② 逐个符号怎么读 → ③ 用具体数字手算一遍
   → ④ 它为什么长这样 → ⑤ 仓库里对应的那几行代码。看不懂时回到 ②。
4. **每章 1–2 小时。** 第 1 部四章可能是全书最慢的部分，因为在补地基；后面越读越快。
5. 需要实操（怎么起训练、怎么回放、怎么建新任务）时，去读
   [单腿站立训练路线图](../single-leg-stand-roadmap-zh.md)——那本讲"怎么做"，这本讲"为什么"。

## 环境准备

```bash
cd ~/Projects/microduck_rl
uv sync                                          # 一次性，装好全部依赖
uv run python docs/learn-zh/labs/ch01_derivative.py   # 跑第 1 章实验
uv run --with pytest pytest tests/               # 仓库自带的 CPU 测试，确认环境健康
```

实验脚本只用仓库已经声明的依赖（numpy、torch、matplotlib、onnxruntime、mujoco、rsl_rl），
不需要额外安装任何东西。图片会保存到 `docs/learn-zh/figures/`。

## 符号速查表（看到不认识的符号先来这里）

**这张表不用背，也不用现在读。** 它只是一张查表：正文里每个符号第一次出现时都会用大白话讲一遍，
只有当你忘了某个符号怎么读、回来翻一下。

### 希腊字母

| 符号 | 读音 | 本书里通常表示 |
|---|---|---|
| α (alpha) | 阿尔法 | 学习率（每次参数调整的步长） |
| β (beta) | 贝塔 | Adam 优化器里的动量系数 |
| γ (gamma) | 伽马 | 折扣因子（未来奖励打几折） |
| δ (delta) | 德尔塔 | TD 误差；小写 δ 也表示"一小段" |
| Δ (Delta) | 大德尔塔 | "变化量"，Δx = x 后 − x 前 |
| ε (epsilon) | 艾普西龙 | 一个很小的正数；PPO 的裁剪范围 0.2；也用作标准正态噪声 |
| θ (theta) | 西塔 | 神经网络的全部参数（权重和偏置打包在一起） |
| λ (lambda) | 拉姆达 | GAE 里的偏差-方差旋钮 |
| μ (mu) | 缪 | 均值 |
| π (pi) | 派 | 策略（policy）：给定观测输出动作分布的函数；也是圆周率 3.14159… |
| σ (sigma) | 西格玛 | 标准差；σ² 是方差 |
| Σ (Sigma) | 大西格玛 | 求和符号（下面单独讲） |
| τ (tau) | 陶 | 一条轨迹（一整段"观测-动作-奖励"序列）；也表示力矩 |
| ∇ (nabla) | 纳布拉 | 梯度（第 3 章） |
| ∂ | 偏（partial） | 偏导数（第 3 章） |

### 运算符号

| 符号 | 怎么读 | 意思 | 例子 |
|---|---|---|---|
| $\sum_{i=1}^{n} x_i$ | "对 i 从 1 到 n，把 x_i 加起来" | 求和 | $\sum_{i=1}^{3} i^2 = 1+4+9 = 14$ |
| $\prod_{i=1}^{n} x_i$ | "把 x_i 连乘" | 连乘 | $\prod_{i=1}^{3} i = 1·2·3 = 6$ |
| $\mathbb{E}[X]$ | "X 的期望" | 平均值（按概率加权） | 骰子的期望是 3.5 |
| $\mathbb{E}_{a\sim\pi}[\,\cdot\,]$ | "a 按 π 采样时，方括号里的东西的期望" | 下标说明随机性来自哪里 | 第 11 章 |
| $x \sim \mathcal{N}(\mu,\sigma^2)$ | "x 服从均值 μ、方差 σ² 的正态分布" | ~ 读作"服从" | 第 4 章 |
| $p(a \mid s)$ | "给定 s 的条件下 a 的概率" | 竖线读作"给定" | 策略 π(a\|s) |
| $\|v\|$ | "v 的范数 / 长度" | 向量长度 | $\|(3,4)\| = 5$ |
| $v \cdot w$ 或 $v^\top w$ | "v 点乘 w" | 点积 | $(1,2)\cdot(3,4) = 11$ |
| $\arg\max_a f(a)$ | "让 f 最大的那个 a" | 不是最大值，是取到最大值的位置 | $\arg\max_a (-(a-2)^2) = 2$ |
| $\approx$ | "约等于" | | |
| $\propto$ | "正比于" | 差一个常数倍 | |
| $\leftarrow$ | "更新为" | 编程里的赋值 `=` | $\theta \leftarrow \theta - \alpha\nabla L$ |
| $\hat{A}$ | "A 帽" | 帽子表示"估计值"，不是真值 | 优势估计 $\hat A_t$ |
| $\bar{x}$ | "x 杠" | 平均值 | |
| $x_t$ | "x 下标 t" | 第 t 步时的 x | 观测 $o_t$、动作 $a_t$ |
| $f'(x)$、$\frac{dy}{dx}$ | "f 撇"、"dy 比 dx" | 导数（第 1 章） | |
| $\text{clip}(x, a, b)$ | "把 x 夹在 a 和 b 之间" | 小于 a 取 a，大于 b 取 b | clip(1.5, 0.8, 1.2) = 1.2 |
| $\min(a,b)$、$\max(a,b)$ | 取小、取大 | | |
| $\exp(x)$ | "e 的 x 次方" | 与 $e^x$ 完全相同的写法 | exp(0) = 1 |
| $\ln x$、$\log x$ | "自然对数" | exp 的逆运算 | ln(e) = 1 |

### 记号约定

- 普通斜体小写字母 $x$ 是一个数；加粗或带箭头的 $\mathbf{x}$ 是一个向量（一串数）。
  本书为了省事，向量也常用普通小写，靠上下文和"维度"一词区分。
- 大写 $W$ 一般是矩阵。
- 下标 $t$ 是时间步；下标 $i$、$j$ 是"第几个元素"。
- 代码引用写成 `文件:行号`，如 `ppo.py:296`。仓库内文件相对 `src/mjlab_microduck/`；
  `rsl_rl/…` 和 `mjlab/…` 在 `.venv/lib/python3.12/site-packages/` 下。

---

准备好了就去 [第 1 章 · 函数与变化率](part1-math/01-函数与变化率.md)。
