# 改写台账：全书按第 2 章标准重写的工作状态

> 这是施工记录，不是教材正文。标准见 [编写规范](编写规范.md)；范文是 [第 2 章](part1-math/02-向量与矩阵.md)。
> 基线：`develop @ 64faf9b`（旧稿用 `git show 64faf9b:<路径>` 取）。2026-09-22 用户要求全部提交：成果已提交到`develop` 分支（上游是 `fork/zh-docs`）；之后的改动照旧先留在工作区，用户说提交再提交。
> 每章任务书的冻结输入用 `uv run --with pytest python tests/test_learn_docs.py --brief chNN` 生成。
> 全书完成后这份文件可以删掉。

## 深度复审（max 档，2026-09-23 起）

全书改写完成后，用户要求把第 5–14 章按 max 档再过一遍（逻辑、概念、讲解细致度、章际衔接）。开工前做了两轮全书只读审查：
接缝、前提节号、承诺兑现、重教、"公式前有没有手算"、口径一致性、数字是否被 check 锁住——**底子干净**，只找出 19 条具体问题（4 高 + 7 中 + 8 低）。
主编直接改的点修：第 4 章（歇脚点、σ 口径表、4.6 的"偏差"换义 ⚠️、"下一章"越界）、第 3 章（补字面值 check）、第 15 章（部首章开篇写法）、第 16 章（head_pose_bias 里的 bias ≠ 偏置）。

| 章 | 深审结果 |
|---|---|
| 05 | 611 → 696 行、15 个 H3、新图 `ch05_autograd.png`（torch 记账 → 倒着走 → 拧旋钮）、check 59 → 62。补了全章最大的卡点：验证损失 0.114 比训练损失 0.146 还低，原稿从不解释。197,788 的口径写清（并纠正主编：全书首现在第 3 章开篇） |
| 06 | 594 → 635 行、9 个 H3、check 67 → 69。补"为什么是 ELU 不是 ReLU"（−0.1 和 −5 的对比数）、点破 W₁ 的两个 2、补"512/256/128 从哪来"、"下一章"补半句对上第 7 章的四样。"层"其实早就是主词——审查误判，已核实 |
| 07 | 616 → 651 行、7.5 拆 4 个 H3、新图 `ch07_adam_two_books.png`、check 72 → 74。统计意义的"偏差"升级成 ⚠️（全书首现在 7.4）、汇率比喻落到 0.14 × 0.9 = 0.126、自测换角度（原与第 13 章重题）、科学计数法当场交代 |

主编另修：第 6 章"项目照 rsl_rl 默认值选 ELU"→ 改成"配置里明确写了"（源码是显式设的）；第 7 章 `**autograd**（自动求导）` 反着用了主词格式 → 改成回指第 5 章 5.4 节。

## 待办（TODO）

**续做从 [改写TODO](改写TODO.md) 开始**：剩下的活、下一步的顺序、标准摘要、验收清单和代理提示词模板都在那里（2026-09-22 收尾时写）。

## 状态

**2026-09-23：全书 19 章 + 附录全部完成并验收**（第 2 章为范文，一字未改）。全量测试 220 passed / 2 skipped / 0 failed。

波次：第 2 章 = 0（范文，不改）；**1** ch03（试点）；**2** ch01、ch04 → 停，等读者过目；
**A** ch05–08；**B** ch09、10、15；**C** ch11、12、17；**D** ch13、14；**E** ch16、18、19。

| 章 | 波次 | 状态 | 旧行数 → 新行数 | 图（旧 → 新） | 备注 |
|---|---|---|---|---|---|
| 01 函数与变化率 | 2 | **已验收（2026-09-22）** | 608 → 1050 | 3 → 10 | 实验 147 → 约 960 行、113 处 check、3 个 TWEAK。评审 1 BLOCKER + 5 MAJOR + 29 MINOR（约 170 个数验算全对），修订落实 BLOCKER/MAJOR 全部、MINOR 27 条（余 2 条评审判可接受）。新增 `ch01_slope.png`；1.12–1.14 加了歇脚点；附录 B 补"变化率 §1.3" |
| 02 向量与矩阵 | 0 | 范文 | 972 | 8 | 不改 |
| 03 多变量与梯度 | 1 | **已验收（2026-09-21）** | 332 → 891 | 2 → 8 | 试点。实验 134 → 1009 行、83 处 check、3 个 TWEAK。评审：35 组手算全对、事实零错误；2 BLOCKER + 9 MAJOR + 29 MINOR，一轮修订全部落实。成本：作者约 59 万 + 修订约 27 万 token，评审约 39 万 token |
| 04 概率与期望 | 2 | **已验收（2026-09-22）** | 370 → 1036 | 1 → 8 | 实验 105 → 约 900 行、137 处 check、3 个 TWEAK。评审 3 BLOCKER（都是"各自舍入后再相加"）+ 8 MAJOR + 26 MINOR，修订全部落实（MINOR 25 条，m24 低优先级未改）。新增 `ch04_ln_curve.png`；自然对数升为主词（主词 13 个）；4.9 的熵 1.4189 改成主线四步手算；"过早收敛"一词不再在本章引出 |
| 05 什么是学习 | A | **已验收（2026-09-22）** | 223 → 611 | 0 → 5 | 实验 58 处 check。评审 0 B + 5 M + 15 m 全改（超参数"不被训练改动"与 adaptive 学习率冲突、📍 的"一轮 = 20 次拧"、沟图、验证集迷你版、Vue computed 类比补"不会自动重算"） |
| 06 神经网络 | A | **已验收（2026-09-22）** | 277 → 594 | 0 → 5 | 实验 68 处 check。评审 1 B（ELU 下限说错）+ 4 M + 19 m 全改；gate_vs_nogate 横轴放宽到 [−2, 2] |
| 07 反向传播与优化器 | A | **已验收（2026-09-22）** | 285 → 616 | 2 → 7 | 实验 73 处 check。评审 0 B + 7 M（含"连调两次 backward() 会报错"）+ 23 m 全改；⚠️ 从 7 处降到 5 处；附录 B 新增"起步修正" |
| 08 输入归一化 | A | **已验收（2026-09-22）** | 245 → 565 | 0 → 7 | 实验 79 处 check。评审 0 B + 2 M（示意数据图注写错、小结讲过头）+ 26 m 全改；头部命令课程终值写进 8.2 |
| 09 强化学习的世界观 | B | **已验收（2026-09-22）** | 268 → 645 | 0 → 6 | 实验 58 处 check。评审 0 B + 6 M（回报图②的运算顺序、2000 局直方图重画并用 0.8⁴ = 0.4096 核对、9.3 ⚠️ 定下"公式里的 s = 网络的输入"、t 与格号下标、TWEAK 后打印的式子仍成立等）+ 23 m（改 20，余 3 条判可接受）。主词 13 个（每节 ≤ 2，按节检查通过）；新增 9.4 策略与探索（兑现第 4 章 4.7） |
| 10 价值函数与贝尔曼方程 | B | **已验收（2026-09-22）** | 236 → 554 | 0 → 6 | 实验 81 处 check。评审 0 B + 3 M（TD 学完后的平均有系统偏差、不是"围着精确值晃"——补表和进阶折叠；和第 9 章设定不同的三处写明，并在 10.2 用反复代入 50 遍兑现第 9 章的 0.7015；V 在 10.3 从真值变成估计，加 ⚠️）+ 14 m（改 11）；按第 9 章 9.3 的口径写"s = 网络的输入"。主编补了 11 处指向第 9 章的小节号 |
| 11 策略梯度 | C | **已验收（2026-09-22）** | 262 → 614 | 1 → 6 | 实验 774 行、73 处 check。改正：日志名 `Mean noise std` → `Mean action std:` / `Policy/mean_std`（`Loss/entropy` 记的是熵本身）；基线实验改用真 V = −0.74（批平均当基线放折叠，(N−1)/N）；动作叫蓝键、绿键，把 A 留给 Â。评审 0 B + 4 M（"步"的两种含义加 ⚠️；面积图重画成 2×2，讲清为什么要面积不变；11.3 拆成 4 个 H3；√(c/2) 只属于推小车）+ 15 m 全改 |
| 12 优势估计 GAE | C | **已验收（2026-09-22）** | 283 → 698 | 1 → 7 | 实验约 870 行、76 处 check。偏差、方差两侧都量化（大步 / 小步玩具；最好的 λ 随平均次数挪，0.94 ≈ 0.95 只是巧合）；`compute_returns()` 12 行已 check；[8, 2] 迷你版兑现第 2 章 2.8。评审 0 B + 7 M（12.7 优势归一化的理由按源码重写，不再和第 7 章 Adam 矛盾；📍 time_outs 注释改准；TWEAK 下图注随变量走；防止把 0.94 读成证明；"第三种偏差"只限本章；开篇对齐第 11 章；12.3 拆 3 个 H3）+ 20 m 全改 |
| 13 PPO | D | **已验收（2026-09-23）** | 308 → 594 | 1 → 5 | 实验 795 行、76 处 check。比率记作 ρ；KL 规则补上下限与 kl_mean > 0；📍 与 rsl_rl 5.0.1 原样一致。评审 0 B + 4 M（小网络演示被写成项目事实 → 改成定性并落到源码；"一步"只指环境步；"日志里常是小负数"标清是推出来的口径；9.0% 的前提说清）+ 6 m 全改。主编同步：附录 B 的"迭代"改登 §7.6（第 14 章 14.1 摆进四只钟），PPO 释义改成"加了一道'别改太多'保险"，比喻冻结为"保险"（不用"保险丝"） |
| 14 训练回路全貌 | D | **已验收（2026-09-23）** | 229 → 740 | 0 → 6 | CPU 伴生实验 `ch14_loop_by_hand.py` 903 行、83 处 check；GPU 实验只改 docstring，存档未动（`learn-zh-rewrite/gpu_archive/`）。作者被中途停下、没交报告，评审替他补了 6 项自检（跨章引用逐个核对、别章对本章的承诺、同形词换义、数字 check、迷你版、外部引用清单）。评审 0 B + 4 M（地图把"环境步"类比成"打了几个回合"、课程的定义引错节号 17.7 → 17.8、第 13 章承诺的"平坦区比例不记"缺失、"两个代表项"其实是三个）+ 10 m 全改 |
| 15 61 维观测 | B | **已验收（2026-09-22）** | 210 → 647 | 0 → 6 | CPU 伴生实验 `ch15_obs_layout.py`（762 行、79 处 check，噪声、延迟、命令范围都从 cfg 读）；GPU 实验 `ch15_print_obs.py` 只改 docstring，输出存档在 scratchpad `gpu_archive/`。删掉了不存在的"61 维断言"。评审 0 B + 8 M（`infer_policy.py` 是 CPU 部署演练脚本、不是真机；0.0135 字面值重算；陀螺仪正负号用右手定则读；15.7 两个 0.01（宽度与 ε）；critic 口径与第 10 章对齐等）+ 17 m（改 14） |
| 16 奖励逐项拆解 | E | **已验收（2026-09-23）** | 285 → 713 | 1 → 5 | 实验 768 行、100 处 check（98 处 CPU）。刻意保住 16.2 / 16.3 / 16.6 / 16.10 四个旧号，附录登记全部仍成立（另新增"核 §16.7"）。评审 2 B（1.01 是实测读数不是上限、EMA 图橙线冲出窄带——改成稳态后再画并加 check）+ 8 M + 10 m（改 9）。改正的事实：三种惩罚在 mjlab 的 `envs/mdp/rewards.py`，项目 `mdp.py` 另有**四个**（主编任务书里写成"一个"，评审纠正）；head_pose_bias 课程 1.0 / 2.0 / 3.0 三档；命令课程放宽到 ±1.1 / ±1.4；0.77 / 1.01 / 15° 是史料并注明出处 |
| 17 执行器与域随机化 | C | **已验收（2026-09-22）** | 281 → 691 | 1 → 7 | 实验 799 行、102 处 check。事实更正：堵转力矩 0.98 → 0.6405 N·m（bam 的 XL330 默认固件电流上限 1.75 A，项目没覆盖；主编与评审各自核实了调用链），"误差 120° 电压饱和 7.5 V"实为 4.92 V，删掉"±0.96 路线图实测"。评审 0 B + 4 M（17.6 简化单关节模型的声明提前、dr_robots 图前后两次抽样矛盾、电流上限补机制和 5 rad/s 手算、m6 参数改为读 json 再 check）+ 18 m 全改 |
| 18 课程学习与 wandb | E | **已验收（2026-09-23）** | 211 → 615 | 1 → 5 | 实验 739 行、77 处 check。评审 0 B + 6 M（命令管理器其实没 deepcopy——源码核实后改写 18.5；时间线图重合圈数说错；换档那一步的奖励仍按旧权重结；删掉一张密度过高的表；6 个数补 check；实验把 20 秒 / 0.02 秒写死改成从 cfg 读，并修出一个把"20 秒"印成"2 秒"的 bug）+ 12 m（改 11）。改正的事实：`pose_command_range_curriculum` 用 `>=`；counter 12,001 正是第 500 圈第一步；`play` 重建环境 → 课程回第 0 档；head_com_range 第 1000 圈起 ±10 mm 封顶 |
| 19 导出与部署 | E | **已验收（2026-09-23）** | 248 → 704 | 0 → 6 | 实验 708 行、57 处 check，固定运行目录 `2026-09-22_13-21-10_learnzh-ch14`（新增 `LEARNZH_RUN_DIR`）。评审 0 B + 13 M（元数据表引错节、对外部仓库的未核实断言、迷你版层数自相矛盾、μ/σ 章内换含义、字面值重算 1.999998、7 个数没 check、全书末段重写）+ 9 m 全改。改正的事实：`ppo.py` 545 行；σ+ε 导出时已算成一个常数；metadata 8 项；两个归一化器分别在各自的 state_dict 里 |
| 附录 D | 2 | 已完成（2026-09-21） | 255 → 263 | 4 | 补了 4 处描述性 alt、4 段「这样读图」、实验清单展开成 6 条；内容未动。指向 ch03/09/15/16/17 的 § 引用待各章定稿后改号 |

每章的改写要点在批准的计划里（`~/.claude/plans/home-joyin-projects-microduck-rl-docs-l-foamy-sloth.md` 的"逐章要点"）。

## 旧 → 新小节对照

（每章验收后填。一个旧号 → 一个或多个新号；`[]` = 删除，要写理由。README / 附录 C、D 里的引用按这张表改号；附录 A、B 按各章交回的清单重建。）

| 章 | 旧 → 新 |
|---|---|
| 03 | 3.1 → 3.0（场景）、3.1；3.2 → 3.2；3.3 → 3.3；3.4 → 3.4（循环、公式、符号表）、3.5（找直线）；3.5 → 3.6；3.6 → 3.7；3.7 → 3.4（为什么沿负梯度会降、小步/大步 301、梯度为零≠学好）、3.3（"最陡"比同样长的一步）、3.7（⚠️ 梯度裁剪≠PPO 裁剪）、3.0（易混词标签），不再单独成节 |

| 04 | 4.1 → 4.1（随机变量、分布、骰子）、4.2（电压、均匀分布、"恰好某值概率为 0"）；4.2 → 4.3（期望）、4.4（方差 / 标准差）；4.3 → 4.5；4.4 → 4.6（钟、密度公式、68/95）、4.7（从钟里抽数、归一化）；4.5 → 4.7（策略的 14 口钟、"给定"）、4.8（log 概率）；4.6 → 4.9；4.7 → 4.2、4.6、4.8；4.8 → 4.8；4.9 → 4.4、4.5；4.10 → 4.0 标签、4.6、4.9 末"回看：全书的三个 σ" |

| 01 | 1.1 → 1.0（地图）、1.1（函数、三台机器）、1.2（函数的图像，原 H3 独立成节）；1.2 → 1.3；1.3 → 1.4（dy/dx 提到主线，极限留折叠）；1.4 → 1.5；1.5 → 1.6；1.6 → 1.7；1.7 → 1.8；1.8 → 1.9（钟形打分）、1.10（宽容度 σ）；1.9 → 1.11；1.10 → 1.12；1.11 → 1.13；1.12 → 1.14；1.13 → 不再单独成节（1.14 末尾一段 + 折叠"第二遍读"，自测分散到 1.12–1.14） |

| 05 | 5.1 → 5.1、5.7；5.2 → 5.2、5.3、5.6；5.3 → 5.4；5.4 → 5.5、5.6；5.5 → 5.7；5.6 → 5.1（手算）、5.2、5.3、5.5、5.6；新增 5.0 |
| 06 | 6.1 → 6.1；6.2 → 6.4；6.3 → 6.2、6.3；6.4 → 6.5；6.5 → 6.6；6.6 → 6.7；6.7 → 拆进 6.1–6.6；新增 6.0 |
| 07 | 7.1 → 7.1、7.2；7.2 → 7.2；7.3 → 7.3 末尾、7.5；7.4 → 7.4（滑动平均）、7.5（Adam）；7.5 → 7.6、7.7；7.6 → 7.7；7.7 → 拆进 7.2、7.3、7.5、7.6；新增 7.0 |
| 08 | 8.1–8.6 → 同号；8.7 → 8.2、8.3；8.8 → 8.4；8.9 → 8.3、8.5、8.6；新增 8.0 |
| 09 | 9.1 → 9.1；9.2 → 9.2（循环）、9.5（轨迹表）；9.3 → 9.3（状态 / 观测、马尔可夫折叠、投影重力自测）、9.5（回合）、9.6（终止 / 超时）；9.4 → 9.7（公式、γ 表、从后往前）、9.8（γ 看多远）；9.5 → 9.9；9.6 → 9.7 开头（[0, 0, 1] 手算）、9.9（奖励 / 回报 / 价值）、9.8（折扣不是只看两秒）、9.3（61 个数）、9.6（24 步自测）；新增 9.0、9.4（策略与探索） |
| 10 | 10.1–10.5 → 同号（10.2 比喻提前、两格手算在前、推导折叠）；10.6 → 10.2（两状态 2 / 1.9 / 1.54）、10.3（H3"TD 目标既有事实也有猜测"，1.045）；10.7 → 10.4（0.75 / 0.25、−1 / −5）、10.5（H3"critic 的标签是谁给的"）；新增 10.0 |
| 11 | 11.1 → 11.1；11.2 → 11.2（一般推导折叠）；11.3 → 11.2 末段"加权的模仿"、11.3；11.4 → 11.4；11.5 → 11.5（高斯策略的梯度移到 11.3）；11.6 → 11.6；11.7（两动作游戏）→ 11.1、11.2，其中"减基线"→ 11.4；11.8（手算）→ 11.3，"随机性不等于不可微"→ 11.1 进阶折叠；新增 11.0 |
| 12 | 12.1、12.2 → 12.1（三步账本的 δ）、12.4（8 步记录与 δ 表）；12.3 → 12.1；12.4 → 12.3（公式、递推、returns）、12.4（8 步的 Â 表、旧图升级）；12.5–12.7 → 同号；12.8（三步账本）→ 12.1–12.3，另加 12.7 的 ⚠️；12.9（n 步目标）→ 12.2 主线、12.3 进阶折叠；12.10（三种边界）→ 12.6；新增 12.0 |
| 13 | 13.1–13.6 → 同号（13.2 重要性采样推导仍折叠；13.3 先逐格手算四格；13.4 价值裁剪提到正文）；13.7（四格）→ 13.3 主线、13.4（min↔max），其"这一项没梯度"H3 → 13.3 进阶折叠；13.8 → 13.6（流程图、"不重新抽"）、13.4（"代理"的由来）；13.9 → 13.5（KL 公式与不对称折叠、⚠️"三把尺"）、13.4（surrogate ≈ 0）、13.3（自测 −1.4）；新增 13.0 |
| 14 | 14.1（一次迭代：五个动作）→ 14.0 地图、14.2 采集、14.3 算优势 + 拧 20 次；14.2（真的跑一次）→ 14.4 一块日志、14.10（冒烟的规模数）；14.3（每一项奖励）→ 14.5 Episode_Reward 的单位、14.6 符号约定；14.4（其余几组）→ 14.7；14.5（产物）→ 14.9；14.6（四只钟）→ 14.1；H3"同一条记录从采集到更新"→ 14.3；H3"从日志算回物理含义"→ 14.5；H3"冒烟测试究竟证明了什么"→ 14.10；新增 14.0、14.8（看曲线：健康的样子与四种常见病）。**主编按标题和内容推定（作者未交报告），评审可修正** |
| 17 | 17.1 → 17.1；17.2 → 17.2（① 位置环）、17.3（② 电机）、17.4（③ 摩擦、电池）、17.6（阶跃图），"时序"拆进 17.4、17.5；17.3 → 17.7（DR 表、抽样时机）、17.8（目标式折叠、三条实践规则成三个 ⚠️）；17.4 → 拆进 17.1–17.3 当迷你版；17.5 → 17.5（延迟、摩擦和动作平滑 → 17.6 的 ⚠️）；17.6 → 17.8；新增 17.0 |
| 15 | 15.1 → 15.1；15.2 → 15.2（陀螺仪、重力方向）、15.3（关节三块）、15.4（为什么加噪声、为什么延迟）；15.3 → 15.5（命令块）、15.6（热切换规则）、15.7（死权重）；15.4 → 15.8；15.5 → 15.9；15.6 → 15.1（单位、0.5 × 0.02）、15.2（坐标系压成两句）、15.3（0.40 − 0.35、"+0.10 vs +0.05"）；15.7 → 15.6（热切换、四元数自测）、15.7（零填充 vs 小范围采样，推导折叠）；旧 📍 的编码器偏置 → 15.3；新增 15.0 |

第 5–8 章的外部引用已同步（2026-09-22）：README 卡点表两行（5.6、6.7 → 5.1、5.3、6.1–6.3；7.7、8.7–8.9 → 7.3、7.5、8.4）；附录 A 四行（h_k、ℝ⁶¹→ℝ¹⁴、α 两义、σ 两义）；附录 B 各章术语逐条改号，另加"优化器 §7.3""滑动平均 §7.4""起步修正 §7.4"。旧稿第 9–19 章里指向第 5–8 章的旧小节号，由各章作者重写时按对照表改（任务书的冻结输入会列出）。

第 1 章的外部引用已同步（2026-09-22）：README 卡点表 1.9–1.13 → 1.11–1.14；附录 A 七行、附录 B 七行改号（链式法则 §1.3 → §1.7 这条老错一并改正；ln 标为"§1.8 先见名字、§4.8 正式讲"）；第 16 章两处 1.9–1.13 → 1.11–1.14。

第 4 章的外部引用已同步（2026-09-22）：README 卡点表 4.7–4.10 → 4.2、4.6、4.9；附录 A 十五行改号、删掉全书从未使用的 ∫ 一行、∏ 改登 §4.8（折叠里）、"同名字母"表两处 §4.10 → §4.6/§4.9；附录 B 十七行改号、新增"log 概率 §4.8"、策略与探索标明"第 9 章正式讲"；第 8 章 4.4 → 4.7、第 9 章 4.5 → 4.7、第 11 章 4.2 → 4.3 与 4.5 → 4.8（4.8 的 ln 导数号码未变）、第 14 章 4.6 → 4.9。

E 波收尾（2026-09-23）：第 16、18、19 章验收。附录 B 新增：核 §16.7、阶梯函数 §18.2、逆向课程 §18.9、算子 §19.2、推理运行时 §19.3、元数据 §19.3；ONNX 改登 §19.1；"数值等价 / 接口一致 / 行为成功"那行改名并改登 §19.10；"环境步"改登 §14.1。README 卡点表三行改号（16.1 / 16.8 / 16.10；18.7、18.8；19.9、19.10），开头定位段改成"先手算再给符号"，目录补两章的 CPU 伴生实验，"本轮校验范围"按 09-23 重写。

D 波收尾（2026-09-23）：WAVE 拆成 13:10、14:11；第 14 章 3 处指回第 13 章补了小节号（13.1、13.2 + 13.3、13.3）；附录 B 把"rollout / 迭代"拆成"迭代 §7.6"和"采集 §14.2"，新增仿真时间 §14.1、墙上时间 §14.1、符号约定 §14.6，易混词表那行 §14.6 → §14.1、§14.2。

C 波收尾（2026-09-22）：WAVE 按定稿顺序拆成 11:7、12:8、17:9；第 12 章 5 处指回第 11 章补了小节号（11.4 四处、11.6 一处）；第 7 章 7.4 那句"本书的'偏差'一直指第 1 章的'指令 − 实际'"改准（第 4 章 4.4 节的"结果 − 平均"也叫偏差），并前指第 12 章统计意义的偏差；附录 B"偏差"条补"第 7 章 7.4 的起步修正修的就是这一种"。

第 11、12、17 章的外部引用已同步（2026-09-22，评审前）：README 卡点表 11.7–11.8 → 11.2–11.3、12.8–12.10 → 12.1–12.3、12.6、17.4–17.6 → 17.1–17.6；附录 A：μ_θ(s) → §11.3，∇π = π∇ln π 改称"对数导数技巧"，新增 𝔼_{a∼π_θ} §11.2、∂ln π/∂μ §11.3、Â §11.4、熵系数 c §11.6，GAE 求和式与 returns → §12.3，λ → §12.3（取值讨论 §12.5），归一化的 Â → §12.3、§12.7，V 公式改用 k_e、q^target，τ → §17.3，ξ 与 DR 目标 → §17.8（折叠里），"V：价值与电压" → §17.2；附录 B：log-derivative trick 改名"对数导数技巧"，新增梯度上升 §11.1、熵奖励 §11.6、n 步回报 §12.2、偏差（统计）§12.5、超时自举 §12.6、优势归一化 §12.7、占空比 §17.2、执行器 §17.4，GAE 改登"广义优势估计 / GAE" §12.3，终止 / 超时 / 采集截断 → §12.6，反电动势 → §17.3，BAM → §17.4，域随机化 → §17.7。

第 10 章的外部引用已同步（2026-09-22）：README 卡点表"第 10 章 10.6–10.7"（这两节已不存在）→ 10.1、10.4；附录 A"V：价值与电压"→ §10.1；附录 B"奖励 / 回报 / 价值 / 优势"→ §9.7、§9.9、§10.1、§10.4。B 波收尾时 WAVE 按定稿顺序拆成 9:4、10:5、15:6，主编给第 10 章补了 11 处、第 15 章补了 4 处指回第 9、10 章的小节号。

第 9、15 章的外部引用已同步（2026-09-22）：README 卡点表"第 9 章 9.7"、"第 15 章 15.1–15.6"；附录 A 的 s/o/a/r → §9.2（o、a、r）、§9.3（s），τ → §9.5，G_t、γ → §9.7，J(π) → §9.9；附录 B 策略、探索 → §9.4，步 / 轨迹、回合 → §9.5，终止 / 超时 → §9.6，回报、折扣因子 → §9.7，本体感受 → §15.2，命令块 → §15.5，零填充 / 死权重 → §15.7，特权观测 → §15.8，热切换 → §15.6（另见第 19 章），新增半开区间 §15.1、右手定则 §15.2；附录 D 三处"§15.6（弧度单位）"→ §15.1。

第 3 章的外部引用已同步（2026-09-21）：README 卡点表 3.7 → 3.4、3.6；附录 A 的 ŷ → §3.5、裁剪系数 → §3.7、∇_θL 补 §3.4；附录 B 的均方误差 → §3.5、梯度裁剪 → §3.7、损失函数 → §1.1/§3.1；第 7 章代码注释里的 3.6 → 3.7。仍然有效、未改：第 5 章"第 3 章 3.4 节"（θ）、附录 D 两处 §3.3。

## 已定稿章节对后文的承诺

| 承诺方 | 对谁 | 内容 |
|---|---|---|
| 第 2 章 2.2 进阶 | 第 3 章 | 讲"梯度方向涨得最快"时用到点积 = 长度 × 长度 × cos(夹角) |
| 第 2 章 末尾 | 第 3 章 | "每个旋钮的导数排成的清单叫梯度"；网络有 197,788 个旋钮，得同时知道每一个往哪拧 |
| 第 2 章 2.2、2.6 | 第 6 章 | 每个神经元先拿自己的"价格表"（权重）和输入算一次点积；线性层后面接激活函数；把四层的旋钮全数出来 |
| 第 2 章 2.7 | 第 8 章 | 不同单位的量凑到一起之前要先缩放——网络输入的尺度问题在第 8 章解决 |
| 第 2 章 2.7 | 第 16 章（已兑现） | 给不同物理量分别设计评分 |
| 第 2 章 2.8 | 第 12 章（已兑现，12.4） | GAE 沿时间轴递推，还要处理回合结束；不能把 A、B 两只机器人的经历误接成一条 |
| 第 2 章 2.1 | 第 15 章（已兑现） | 逐项列出 61 维观测这张清单 |
| 第 3 章 3.4 | 第 11 章（已兑现，11.1） | 想让一个数变大就把减号换成加号、顺着梯度走——策略梯度用的是加号版 |
| 第 3 章 3.5 | 第 4、5、7 章 | 训练机器人时数据不是现成的、带随机——第 4 章从这里接着讲；第 5 章起梯度交给 torch 自动算；第 7 章讲它是怎么算的（反向传播） |
| 第 3 章 3.6、3.7、📍 | 第 7 章 | 讲"优化器"是什么；Adam 让每个旋钮有自己的步长，解决"一个 α 顾不了陡缓两个方向"。第 7 章旧稿 7.3"扁碗"与新 3.6"碗越陡门槛越低"重叠，重写时回指 3.6，不要重教 |
| 第 3 章 🧪 改一改 | 第 8 章 | 把 X 的范围放大 10 倍、α = 0.1 就发散——这是"输入归一化"的伏笔；第 8 章旧稿 8.1 引用了第 3 章九行表的数字（2.02442 / 1.00206 / 0.00993478），要保持呼应 |
| 第 3 章 3.0、3.7、📍 | 第 13 章 | PPO 的裁剪剪的是另一样东西；KL 这把尺子；自适应学习率 |
| 第 3 章 📍 | 第 10 章（已兑现，10.5） | critic（价值函数）是另一张网络 |
| 第 1 章 1.8 | 第 4 章（已兑现，4.8） | 正式讲 ln；高斯分布是钟形打分的"孪生兄弟" |
| 第 1 章 1.7 | 第 6、7 章 | 网络 = 几十台机器串成的流水线；反向传播 = 从最外层一台台把倍数往回乘（`loss.backward()`） |
| 第 1 章 1.14 折叠 | 第 9–13 章 | 训练到底对哪台机器求导：分数是采样数据、不沿仿真往回求导；打分不光滑也能学 |
| 第 1 章 1.14 | 第 16 章（已兑现，16.8） | 头部摆动这类"改不掉的偏差"不该用窄钟去罚 |
| 第 4 章 4.7、4.9 | 第 8 章 | 对网络输入做的就是 (x−μ)/σ 这一步（本书叫归一化）；那里还会遇到第三个 σ |
| 第 4 章 4.7 | 第 9 章（已兑现，9.4）、第 11 章 | 正式定义"策略"；和第 11 章一起正式讲"探索" |
| 第 4 章 4.5 | 第 10 章（已兑现，10.1） | "蒙特卡洛估计"这个名字会再见到 |
| 第 4 章 4.3、4.5、4.7–4.9 | 第 11 章（已兑现） | 期望的线性 → "减基线不影响方向"；σ/√n → "方差大就要更多样本"；熵奖励细讲；重参数化；log 密度对 μ 的导数 (x−μ)/σ²；∇log π = ∇π/π（4.8 折叠）。旧稿第 11 章还写着过期的日志名 `Mean noise std`，重写时改成 `Mean action std` / `Policy/mean_std`；第 11 章旧稿 140 行、第 14 章旧稿 120 行用了"过早收敛"——"收敛"要当场用大白话讲（第 4 章已不再引出这个词） |
| 第 4 章 4.6、4.8 | 第 13 章 | 两个 π 同页出现；PPO 用两个 log 概率相减再 exp 得到密度比 |
| 第 4 章 4.2 | 第 17 章（已兑现，17.7） | 域随机化：电池电压 U(6.5, 8.2) 开机抽一次 |
| 第 4 章 4.7 | 第 19 章（已兑现，19.5） | 部署时不加噪声，只用均值 μ |
| 第 5 章 📍 | 第 13、14 章（已兑现） | 口径："一轮 = 收一次数据 + `update()` 拧 5 × 4 = 20 次"（num_learning_epochs = 5、num_mini_batches = 4） |
| 第 5 章 | 第 12、17、18 章 | 第 12 章讲 `compute_returns`；第 17 章把域随机化当过拟合的对策；第 18 章讲怎么读训练曲线、何时停 |
| 第 6 章 | 第 15 章（已兑现，15.8） | 逐项讲 critic 多出的 15 项（76 = 61 + 15）；现在 CPU 实验 `ch15_obs_layout.py` 第 8 节也能核对，不再只靠 GPU |
| 第 7 章 7.6 折叠 | 第 13 章 | 同一批记录用 5 遍、每遍用当前网络重算 log 概率；`detach()` |
| 第 8 章 8.2 | 第 15 章（已兑现）、第 18 章 | 头部命令课程：第 2000 次迭代起 ±1.10 / ±1.10 / ±1.40 / ±0.31 rad；身体命令只有一档 ±0.005 m / ±0.05 rad——两章说法要一致 |
| 第 8 章 📍 | 第 19 章（已兑现，19.3） | 真的打开 ONNX 看归一化这一层 |
| 第 9 章 9.3 | 第 10 章（已兑现） | 口径：公式里的 s = 网络的输入（策略看 61 个数、critic 看 76 个数），指全部真相时用文字写"状态"；V(s)、Q(s, a) 沿用 |
| 第 9 章 9.7、9.9 | 第 10 章（已兑现：10.1；0.7015 在 10.2） | 价值 = 很多局回报的平均（走廊起点约 0.70）；不抽样算出精确值的办法；从后往前的规则 |
| 第 9 章 9.4、9.9 | 第 11 章（已兑现） | 把平均回报变成能求梯度的东西；探索怎样推着钟的中心往好的动作挪 |
| 第 9 章 9.6、📍 | 第 12 章（已兑现，12.6、📍） | 超时之后、以及 24 步采集切片末尾的分，用 critic 的估计补上；逐行读 `rsl_rl/algorithms/ppo.py` 里超时补分的那几行 |
| 第 9 章 | 第 16、17 章 | 十几项打分（第 16 章）；舵机延迟属于环境（第 17 章，已兑现，17.5） |
| 第 15 章 | 第 16 章（已兑现） | 身体命令的奖励权重是 0；让动作平滑的那一项罚的是相邻两步动作的差 |
| 第 15 章 | 第 17 章（已兑现，17.7、17.8） | IMU 装歪属于域随机化 |
| 第 15 章 | 第 18 章（已兑现） | 头部命令为什么从小范围起步；全零速度命令的比例从 2% 涨到 25% |
| 第 10 章 10.4 | 第 11 章（已兑现） | 把正优势动作的概率推高；抽样怎么求导 |
| 第 10 章 10.3、10.5 | 第 12 章（已兑现） | λ 在真实奖励和估计之间折中；returns 由 δ 算出、λ = 0 时就是 TD 目标（10.5 说项目的 critic 标签是 λ = 0.95 的升级版）；到点补 γV；从一串 δ 估优势 |
| 第 10 章 | 第 13 章 | `surrogate_loss` 是 actor 的损失 |
| 第 10 章（依赖） | 第 9 章 | 第 10 章用了第 9 章的 0.703、0.7015、走满 50 步、"总是按 →"、"随机乱按"和 9.3 折叠里的马尔可夫性质——改第 9 章这些地方要同步第 10 章 |
| 第 11 章 | 第 12 章（已兑现，12.3） | Â 由一串 δ 算出的升级版 |
| 第 11 章 | 第 13 章 | 用存下的旧 ln π 做比较，比率被夹住 |
| 第 11 章 | 第 14 章 | 真实日志里的 `Mean action std:` 和 `Loss/entropy`（开始时约 19.86） |
| 第 12 章（依赖） | 第 11 章 | 第 12 章 12.5、12.7 引用第 11 章"基线不改变平均的推动方向" |
| 第 12 章 | 第 13 章 | PPO 的"别改太多"保险；同一批数据用 5 遍；用的是归一化后的 advantages |
| 第 17 章 | 第 16 章（已兑现） | `dof_pos_limits`（权重 −1.0）；"罚相邻两步动作之差"的那一项 |
| 第 17 章 | 第 18 章（已兑现） | 质心课程 ±3 → ±15 mm；"课程"这个词在第 18 章正式讲 |
| 第 17 章 | 第 19 章（已兑现） | 部署时用钟的均值；训练和部署的滤波要一致（策略本身不带动作滤波） |
| 第 13 章 | 第 14 章（已兑现，14.4） | 对照冒烟训练日志逐行说清每个数；Mean surrogate loss 常是小负数；rsl_rl 不记平坦区比例 |
| 第 15 章 | 第 19 章（已兑现，19.6 热切换图） | 用 `observation_names` 核对布局；热切换（15.6）的伏笔 |

## 共用例子与比喻（第 2 部开工前冻结）

- 第 2 章已立的比喻：向量 = 购物清单；点积 = 清单 × 价格 = 总价；矩阵 = 几家店的价格表；偏置 = 运费；轴 = 查找时要回答的一个问题。后面各章优先沿用。
- 待冻结：走廊游戏的设定（第 9–10 章）、直线拟合数据（第 3、5 章）、钟形打分核（第 1、4、16 章）、汇率（第 1、7 章）、"别改太多"的一道保险（第 13 章，**已定稿：正文一律说"保险"，不用"保险丝"**）。

## 引用同步状态

| 文件 | 指向哪些章 | 状态 |
|---|---|---|
| README「这一版怎样把基础讲透」卡点表 | 1–19 | **全部已改号**（另改了开头定位段、目录、本轮校验范围） |
| 附录 A「首次出现」列 | 1–19 | **全部已改号**（原来约 10 处就错的锚点一并改正） |
| 附录 B「首次出现」列 | 1–19 | **全部已改号并过语义检查**（检查器逐条核对"本章第一次加粗介绍它"的小节） |
| 附录 C | 2 | 指向第 2 章，无需改 |
| 附录 D | 2、3、15、16、17 | **全部核对过**：§15.1、§16.6 / §16.10、§17.1、§3.3 都仍指对地方 |

## 施工须知（换会话续做时先读）

- **2026-09-22 起**：用户决定第 1 部不停、不过目，按原计划一路跑；并要求省 token。流水线改为：精简作者（任务书 `briefs/COMMON.md` 精简版 + `briefs/EXEMPLAR.md`，不通读第 1–4 章，≤ 70 次调用）→ 主编机械验收 → **评审兼修订**一个代理（`briefs/REVIEWER.md`，≤ 50 次调用）→ 主编验收、同步外部引用、快照。主编会话见下一条（现在是 [3e6e03]）。

- **2026-09-22 的决定（用户原话）**：08:39"继续，这回不要浪费token了"；08:44"我不用过目，按照原计划跑就好"——第 1 部之后**不停**，按波次一路做完。10:00 用户补充"省token 但是要以内容高效优质为主"——**质量第一**，调用次数、行数、图数只作参考。省 token 的做法：作者只读编写规范 + `briefs/EXEMPLAR.md`（第 3 章节选），≤ 70 次调用；评审与修订合成一个代理（`briefs/REVIEWER.md`，≤ 50 次调用）；不做第二轮评审；主会话少轮次、批量做事。旧版任务书留在 `briefs/COMMON_v1.md`、`briefs/REVIEWER_v1.md`。08:38–09:14 的主编是恢复出来的会话 [92c900]（已结束）；09:52 用户回到原会话 [3e6e03]、换 Opus 5，让它接着做——**现在的主编是 [3e6e03]**。
- **⚠️ 同一时间只能有一个主编会话。** 2026-09-21 21:02，这段对话被恢复成了第二个同名会话（[334799]），恢复时 harness 把后台的第 1、4 章作者代理按原 ID 又各拉起一份，于是每章出现两个作者实例（它们自己协调成"一个写、一个只读"，留言在 `scratchpad/ch01/README_两个作者实例.md`、`scratchpad/ch04/README_第二位作者B已退出.md`）。裁决规则：**启动更早的会话当主编**（`ListAgents` 能看到启动时间）——只有主编改共享文件（README、附录、`tests/`、规范、台账、`labs/_common.py`、`labs/_draw.py`、briefs）、做验收、启动评审；另一个会话不改共享文件、不启动代理，把自己名下代理的交稿报告转给主编。用户的直接指示优先于这条规则。恢复旧对话续做之前，先确认原会话已经结束。

- 每章流水线：全新作者代理（任务书 = `briefs/COMMON.md` + `briefs/chNN.md`，冻结输入用 `--brief chNN` 生成）→ 主编核对改动路径、复跑实验、通读、逐张看图 → 全新评审代理（不看旧稿）→ 作者代理按意见修订（SendMessage 续跑，保留上下文）→ 验收 → 快照。
- 任务书长期存放在 `~/.claude/projects/-home-joyin-Projects-microduck-rl/learn-zh-rewrite/briefs/`。
- 开工前在 `tests/test_learn_docs.py` 里把该章加入 `REWRITTEN` 和 `PENDING_SYNC`、填 `WAVE`；验收并同步完外部引用后移出 `PENDING_SYNC`、加入 `B_SYNCED`。
- **用量上限会打断代理**（09-21 17:50 第 3 章修订中途一次；09-21 21:23 第 1、4 章写到一半时又一次；09-22 约 02:30 第三次；09-22 约 11:00 第四次（A 波四个评审兼修订代理同时中断，11:40 重置后续做）；09-22 约 14:25 第五次（C 波第 11、12 章的评审中断在改完正文、还没改实验处，16:40 重置后续做）——21:23 那次因为有重复会话和重复代理，一个 5 小时窗口 43 分钟就烧完了）。粗略账：一个窗口约 200 万 token；一章 ≈ 作者 60 万 + 评审 40 万 + 修订 30 万。**瓶颈是 token 额度，不是并行度**——并行不省 token，只省时钟时间。成果都在磁盘上：先看文件修改时间、带 `LEARNZH_FIG_DIR` 复跑实验、跑检查器报告，弄清停在哪一步，再用 SendMessage 让同一个代理续做。并行的代理越多，上限来得越快。
- **派评审前先用 `ListAgents` 确认作者代理已经收工。** 09-22 11:45 第 5 章的作者代理在评审进行时又续做了一轮（改得对，但两人同时改一章有互相覆盖的风险）；当时靠通知评审"以磁盘上的现文字为准"避开了冲突。
- **波次收尾时拆开 WAVE**：同一波的章互相不能带小节号引用（检查器按 WAVE 判，作者只能写"第 N 章（概念名）"）。整波定稿后，主编按定稿顺序把 WAVE 拆开（B 波：9:4、10:5、15:6），再给指回同波更早章节的引用补小节号；下一波取更大的数（C 波 = 7）。
- **09-22 14:25–16:42（用量上限暂停期间），docs/learn-zh 与 tests 的全部改动被整体 git add 进了暂存区**——不是主编会话或子代理做的（当时都停着）。工作区内容不受影响；我们不 unstage（那也是改状态的 git 命令），等用户定——**已解决**：用户随后要求全部提交，已提交到`develop` 分支（上游是 `fork/zh-docs`）。看改动用 `git status` 或 `git diff HEAD`；只看 `git diff` 会漏掉已暂存的部分。
- 验证性运行一律带 `LEARNZH_FIG_DIR`；正式图只在实验 0 个 ✗ 时、不带环境变量生成。

## 第 1 部评审指引（给读者：先读哪、看什么）

建议先完整读**第 3 章**——它是试点，走完了"作者 → 主编通读 → 独立评审 → 修订"的整条流水线，后面各章都照它的样子做。

### 第 3 章 · 多变量与梯度（332 → 891 行；2 → 8 张图；实验 134 → 1009 行、83 处 check）

- **结构变化**：新增 3.0 地图；旧 3.4 拆成 3.4（梯度下降）和 3.5（找直线）；旧 3.5 → 3.6、旧 3.6 → 3.7；旧 3.7"详解"拆散并回 3.3 / 3.4 / 3.7，不再单独成节。
- **贯穿全章的一组小数字**：站在 (1, 2) → 高度 13 → 两个偏导数 2 和 12 → 梯度 (2, 12) → 走一小步到 (0.98, 1.88)、高度 11.5636。正文、8 张图、实验用的是同一组数。
- **比喻**：淋浴的两个旋钮（3.0）→ 一只碗、等高线（3.1）→ 把碗切一刀（3.2）→ 梯度当"价格表"和当"箭头"两种用法（3.3，接第 2 章的清单 × 价格）→ 每步"离碗底的距离乘同一个数 1 − 2α"（3.6）。
- **新补的手算**：一个旋钮走一步（w: 1 → 1.4，损失 4 → 2.56）；两个旋钮走一步（预计降 1.48、实际降 1.4364）；3 个数据点手算一整轮找直线（损失 22 → 6.16）；三种学习率各手算三步（稳稳靠近 / 来回跳着靠近 / 越跳越远）；梯度 (3, 4) 裁成 (0.6, 0.8)。
- **折叠起来的**：点积的几何意义（兑现第 2 章 2.2 的承诺）、负梯度下降的一般公式、均方误差偏导数的链式法则推导、梯度为零 ≠ 学好了、局部极小与鞍点。
- **⚠️ 四处**："梯度算对了"≠"这一步一定变好"；x、y 在 3.5 节从旋钮变成数据；梯度裁剪 ≠ PPO 裁剪；（3.0 地图里先贴了四条易混词标签）。
- **图**：`ch03_overview`（四步总览）、`ch03_bowl_contours`（高度表 → 连成圈 → 从上往下看碗）、`ch03_partial_slice`（切一刀量斜率）、`ch03_gradient_arrows`（箭头垂直穿过等高线 + 12 个方向各走一步）、`ch03_descent_path`（小步一路下坡 / 大步跨到对面 301）、`ch03_line_fit`（数据、(w, b) 的碗、损失曲线三联）、`ch03_learning_rate`（三种 α 的轨迹）、`ch03_gradient_clipping`（只缩短不转向）。
- **改正的事实**：映射块里旧稿引用的 `clip_grad_norm_(self.actor.parameters(), 1.0)` 在源码里并不存在（真实是 `self.max_grad_norm`，且 actor、critic 各裁一次）；"α = 1 开始来回跳"实为缓慢发散（门槛 0.9957）；旧稿几处"实验输出"块是手改过的。现在实验最后一节会核对映射块引用的 12 行源码原样存在。
- **请你特别留意**：3.3 节偏长（两种用法 + 12 方向实验 + 折叠）；3.5 节从"现成的碗"过渡到"数据造出来的碗"是全章最难的一跳；整章 891 行，读着累不累、哪里想跳过。

（第 1、4 章待补。）

## 停顿点要问读者的事

（第 1 部做完时填：术语裁决、风格问题、每章耗时。）
